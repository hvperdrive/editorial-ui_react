import { useEffect, useState } from 'react';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import CharacterCount from './CharacterCount';

<Meta title="CharacterCount" component={CharacterCount} />

# CharacterCount

A vertically scrollable tab bar

## Preview of component

<Preview>
	<Story name='Base'>
		<CharacterCount min={10} max={20} />
	</Story>
	<Story name='Min'>
		<CharacterCount min={10} />
	</Story>
	<Story name='Max'>
		<CharacterCount max={20} />
	</Story>
	<Story name='Count valid'>
		<CharacterCount count={15} min={10} max={20} />
	</Story>
	<Story name='Count invalid'>
		<CharacterCount count={150} min={10} max={20} />
	</Story>
</Preview>

## Props

<Props of={CharacterCount} />
