import { action } from '@storybook/addon-actions';
import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';



import {
	STATUS_MOCK_ACTIVE,
	STATUS_MOCK_INACTIVE,
	STATUS_MOCK_ARCHIVED,
} from './Status.mock';
import Status from './Status';

<Meta title="Status" component={Status} />

# Status

Some explanation about our Status component

### Different variations

<Canvas isColumn>
	<Story name="Active">
		<Status
			label={STATUS_MOCK_ACTIVE.label}
			type={STATUS_MOCK_ACTIVE.type}
		/>
	</Story>
	<Story name="Inactive">
		<Status
			label={STATUS_MOCK_INACTIVE.label}
			type={STATUS_MOCK_INACTIVE.type}
		/>
	</Story>
	<Story name="Archived">
		<Status
			label={STATUS_MOCK_ARCHIVED.label}
			type={STATUS_MOCK_ARCHIVED.type}
		/>
	</Story>
</Canvas>

## Props

<ArgsTable of={Status} />
