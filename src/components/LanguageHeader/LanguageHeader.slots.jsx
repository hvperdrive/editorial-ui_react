import React from 'react';

import { SLOT_PROP_TYPES } from '../../const';

export const LanguageHeaderFormSection = ({ children }) => <>{children}</>;
LanguageHeaderFormSection.propTypes = SLOT_PROP_TYPES;
