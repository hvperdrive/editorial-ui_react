import { useRef, useState } from 'react';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';


import Tooltip from './Tooltip';
import { TooltipTypeMap } from './Tooltip.const';

<Meta title="Tooltip" component={Tooltip} />

# Tooltip

Some explanation about our Tooltip component

### Different variations

<Preview isColumn>
	<Story name="Default">
		{() => {
			const buttonRef = useRef(null);
			const [isVisible, setVisibility] = useState(false);
			return (
				<>
					<button
						ref={buttonRef}
						onMouseEnter={() => setVisibility(true)}
						onMouseLeave={() => setVisibility(false)}
					>
						Default tooltip
					</button>
					<Tooltip isVisible={isVisible} targetRef={buttonRef}> Some tooltip info </Tooltip>
				</>
			);
		}}
	</Story>
	<Story name="Primary">
		{() => {
			const buttonRef = useRef(null);
			const [isVisible, setVisibility] = useState(false);
			return (
				<>
					<button
						ref={buttonRef}
						onMouseEnter={() => setVisibility(true)}
						onMouseLeave={() => setVisibility(false)}
					>
						Primary tooltip
					</button>
					<Tooltip type={TooltipTypeMap.PRIMARY} isVisible={isVisible} targetRef={buttonRef}> Some tooltip info </Tooltip>
				</>
			);
		}}
	</Story>
	<Story name="Secondary">
		{() => {
			const buttonRef = useRef(null);
			const [isVisible, setVisibility] = useState(false);
			return (
				<>
					<button
						ref={buttonRef}
						onMouseEnter={() => setVisibility(true)}
						onMouseLeave={() => setVisibility(false)}
					>
						Secondary tooltip
					</button>
					<Tooltip type={TooltipTypeMap.SECONDARY}  isVisible={isVisible} targetRef={buttonRef}> Some tooltip info </Tooltip>
				</>
			);
		}}
	</Story>
	<Story name="Tooltip placement">
		{() => {
			const buttonRefTop = useRef(null);
			const buttonRefBottom = useRef(null);
			const buttonRefLeft = useRef(null);
			const buttonRefRight = useRef(null);
			const [isVisibleTop, setVisibilityTop] = useState(false);
			const [isVisibleBottom, setVisibilityBottom] = useState(false);
			const [isVisibleLeft, setVisibilityLeft] = useState(false);
			const [isVisibleRight, setVisibilityRight] = useState(false);
			return (
				<>
					<div className="u-margin-top-lg">
						<button
							ref={buttonRefTop}
							onMouseEnter={() => setVisibilityTop(true)}
							onMouseLeave={() => setVisibilityTop(false)}
						>
							Position top
						</button>
						<Tooltip placement="top" isVisible={isVisibleTop} targetRef={buttonRefTop}> Some tooltip info </Tooltip>
					</div>
					<div className="u-margin-top-lg">
						<button
							ref={buttonRefBottom}
							onMouseEnter={() => setVisibilityBottom(true)}
							onMouseLeave={() => setVisibilityBottom(false)}
						>
							Position bottom
						</button>
						<Tooltip placement="bottom" isVisible={isVisibleBottom} targetRef={buttonRefBottom}> Some tooltip info </Tooltip>
					</div>
					<div style={{marginLeft: "200px"}} className="u-margin-top-lg">
						<button
							ref={buttonRefLeft}
							onMouseEnter={() => setVisibilityLeft(true)}
							onMouseLeave={() => setVisibilityLeft(false)}
						>
							Position left
						</button>
						<Tooltip placement="left" isVisible={true} targetRef={buttonRefLeft}> Some tooltip info </Tooltip>
					</div>
					<div className="u-margin-top-lg">
						<button
							ref={buttonRefRight}
							onMouseEnter={() => setVisibilityRight(true)}
							onMouseLeave={() => setVisibilityRight(false)}
						>
							Position right
						</button>
						<Tooltip placement="right" isVisible={isVisibleRight} targetRef={buttonRefRight}> Some tooltip info </Tooltip>
					</div>
				</>
			);
		}}
	</Story>
</Preview>

## Props

<Props of={Tooltip} />
