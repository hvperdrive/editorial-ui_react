import { Fragment } from 'react';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Filter from './Filter';
import { TextField } from '@acpaas-ui/react-components';
import { FilterBody } from './Filter.slots.jsx';

<Meta title="Filter" component={Filter} />

# Filter

A generic filter component that can be used anywhere

## Preview of component

<Preview>
	<Story name='Base'>
		<Filter title='Filter' onConfirm={action()} onClear={action()} onFilterRemove={action()}>
			<FilterBody>
				<div className="col-xs-8">
					<TextField
						label="zoeken"
						id="zoeken"
						name="zoeken"
						className="textfield-class"
						placeholder={`Zoeken op`}
						onChange={(value) => console.log('Filteren op:', value)}
						iconright="search"
					/>
				</div>
			</FilterBody>
		</Filter>
	</Story>
	<Story name='Multiple input fields'>
		<Filter title='Filter' onConfirm={action()} onClear={action()} onFilterRemove={action()}>
			<FilterBody>
				<div className="col-xs">
					<div className="row">
						<div className="col-xs">
							<TextField
								label="zoeken"
								id="zoeken"
								name="zoeken"
								className="textfield-class"
								placeholder={`Zoeken op`}
								onChange={(value) => console.log('Filteren op:', value)}
								iconright="search"
							/>
						</div>
						<div className="col-xs">
							<TextField
								label="zoeken"
								id="zoeken"
								name="zoeken"
								className="textfield-class"
								placeholder={`Zoeken op`}
								onChange={(value) => console.log('Filteren op:', value)}
								iconright="search"
							/>
						</div>
					</div>
				</div>
			</FilterBody>
		</Filter>
	</Story>
	<Story name='No input fields'>
		<Filter title='Filter' onConfirm={action()} onClear={action()} onFilterRemove={action()} />
	</Story>
	<Story name='Active filter items'>
		{() => {
			const array = [{label:'Naam', value:'Lorem'}, {label:'iets', value: 'Ipsum'}];
			return(
				<Filter title='Filter' onConfirm={action('add filter')} onClear={action('remove all filters')} activeFilters={array} onFilterRemove={action('remove filter item')} />
			)
		}}
	</Story>
</Preview>

## Props

<Props of={Filter} />
