import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Filter from './Filter';
import { TextField, Datepicker } from '@acpaas-ui/react-components';
import { FilterBody } from './Filter.slots';

<Meta title="Filter" component={Filter} />

# Filter

A generic filter component that can be used anywhere

## Preview of component

<Preview>
	<Story name='Base'>
		<Filter
			title='Filter'
			noFilterText='Geen filters beschikbaar'
			onConfirm={action()}
			onClear={action()}
			confirmText='Toepassen'
			cleanText='Alles leegmaken'
			onFilterRemove={action()}
		>
			<FilterBody>
				<div className="col-xs-12 col-sm">
					<TextField
						label="zoeken"
						id="zoeken"
						name="zoeken"
						placeholder="Zoeken op"
						iconright="search"
					/>
				</div>
			</FilterBody>
		</Filter>
	</Story>
	<Story name='Multiple input fields'>
		<Filter
			title='Filter'
			noFilterText='Geen filters beschikbaar'
			onConfirm={action()}
			confirmText='Toepassen'
			cleanText='Alles leegmaken'
			onClear={action()}
			onFilterRemove={action()}
		>
			<FilterBody>
				<div className="col-xs-12 col-sm">
					<div className="row">
						<div className="col-xs-6">
							<TextField
								label="zoeken"
								id="zoeken"
								name="zoeken"
								iconright="search"
							/>
						</div>
						<div className="col-xs-6">
							<TextField
								label="Voornaam"
								id="firstname"
								name="firstname"
							/>
						</div>
					</div>
					<div className="row u-margin-top">
						<div className="col-xs-6">
							<TextField
								label="Naam"
								id="lastname"
								name="lastname"
							/>
						</div>
						<div className="col-xs-6">
							<Datepicker
								locale="en"
								id="datepicker"
								name="date"
								label="Date"
								format="DD/MM/YYYY"
								readOnly
							/>
						</div>
					</div>
				</div>
			</FilterBody>
		</Filter>
	</Story>
	<Story name='No input fields'>
		<Filter
			title='Filter'
			noFilterText='Geen filters beschikbaar'
			onConfirm={action()}
			onClear={action()}
			confirmText='Toepassen'
			cleanText='Alles leegmaken'
			onFilterRemove={action()}
		/>
	</Story>
	<Story name='Active filter items'>
		{() => {
			const array = [{valuePrefix:'User', value:'John Doe'}, {valuePrefix:'Status', value: 'Success'}];
			const deleteFilter = (filter) => {action()(filter)};
			return(
				<Filter
					title='Filter'
					noFilterText='Geen filters beschikbaar'
					onConfirm={action('add filter')}
					onClean={action('remove all filters')}
					confirmText='Toepassen' cleanText='Alles leegmaken'
					activeFilters={array} onFilterRemove={deleteFilter}
				/>
			)
		}}
	</Story>
</Preview>

## Props

<Props of={Filter} />
