import { renderChildren } from '../../helpers';

export const ControlledModalHeader = renderChildren;
export const ControlledModalBody = renderChildren;
export const ControlledModalFooter = renderChildren;
