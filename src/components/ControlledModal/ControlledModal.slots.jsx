import { SLOT_PROP_TYPES } from '../../const';

export const ControlledModalHeader = ({ children }) => children;
ControlledModalHeader.propTypes = SLOT_PROP_TYPES;

export const ControlledModalBody = ({ children }) => children;
ControlledModalBody.propTypes = SLOT_PROP_TYPES;

export const ControlledModalFooter = ({ children }) => children;
ControlledModalFooter.propTypes = SLOT_PROP_TYPES;
